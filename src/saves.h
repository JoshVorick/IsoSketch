#pragma one

/*
 * This handles all functions related to saving and or loading
 * TODO: Save and load each prompt for a file name
 */

#include "dots.h"

void save(progVars *pv);
void load(progVars *pv);
